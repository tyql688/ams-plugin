# --------------------------------------------------------
# 命令前缀配置
# --------------------------------------------------------

# 前缀列表：触发命令时需要输入的起始词
# 示例：鸣潮设置
prefixes:
  - "鸣潮"
  - "mc"
  - "ams"

# 是否允许空前缀：关闭则强制要求输入上述前缀之一
allowEmptyPrefix: false
